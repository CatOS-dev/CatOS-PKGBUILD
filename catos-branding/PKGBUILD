pkgname=catos-branding
pkgver=1.2
pkgrel=1
pkgdesc="CatOS branding"
arch=('any')
url='https://github.com/CatOS-dev/catos-branding'
license=('GPL')
makedepends=('git')
install="${pkgname}.install"
source=('git+https://github.com/CatOS-dev/catos-branding.git')
sha256sums=('SKIP')

package() {
  cd "${srcdir}/${pkgname}"

  if [[ -d etc ]]; then
    install -d "${pkgdir}/etc"
    cp -a etc/. "${pkgdir}/etc/"
  fi
  if [[ -d usr ]]; then
    install -d "${pkgdir}/usr"
    cp -a usr/. "${pkgdir}/usr/"
  fi
}
