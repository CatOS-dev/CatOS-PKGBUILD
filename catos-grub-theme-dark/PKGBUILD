pkgname=catos-grub-theme-dark
pkgver=25.02
pkgrel=3
pkgdesc='GRUB dark theme for CatOS'
arch=('any')
url='https://github.com/CatOS-dev/catos-grub-theme-dark'
license=('GPL-3.0-only')
depends=('grub')
makedepends=('git')
source=("git+https://github.com/CatOS-dev/catos-grub-theme-dark.git")
sha256sums=('SKIP')

package() {
  cd "${srcdir}/${pkgname}"

  install -d "${pkgdir}/usr"
  cp -a usr/. "${pkgdir}/usr/"
}
