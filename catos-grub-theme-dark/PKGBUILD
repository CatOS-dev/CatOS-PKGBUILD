pkgname=catos-grub-theme-dark
pkgver=25.02
pkgrel=3
pkgdesc='GRUB dark theme for CatOS'
arch=('any')
url='https://github.com/CatOS-Home/CatOS'
license=('GPL-3.0-only')
depends=('grub')
makedepends=('git')
source=("https://bdd.invalid/catos-grub-theme-dark.git")
sha256sums=('SKIP')

package() {
  cd "${srcdir}/${pkgname}"

  install -d "${pkgdir}/usr"
  cp -a usr/. "${pkgdir}/usr/"
}
