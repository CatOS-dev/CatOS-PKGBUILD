# Enable CatOS automatic transaction snapshots.
ENABLED=yes

# Create a snapshot when at least this many unique packages are changed in one
# transaction. Set to 0 to disable the package-count trigger. Boot-critical
# path changes always trigger while ENABLED=yes.
PACKAGE_THRESHOLD=25

# Snapper configuration used for the system root.
SNAPPER_CONFIG=root
