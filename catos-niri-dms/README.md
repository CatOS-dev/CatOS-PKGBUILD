# catos-niri-dms

CatOS user skeleton for a Niri session powered by Dank Material Shell and Ghostty.

The package vendors the DMS 1.5.1 compositor configuration so builds are reproducible.
DMS-managed fragments live in `~/.config/niri/dms/`; the main config includes them
as optional files so a partially regenerated configuration can still start.
