pkgname=catos-kde-settings
pkgver=2.0
pkgrel=2
pkgdesc="CatOS KDE Settings"
arch=('any')
url='https://github.com/CatOS-dev/catos-kde-settings'
license=('GPL')
install="${pkgname}.install"
makedepends=('git')
source=("git+https://github.com/CatOS-dev/catos-kde-settings.git")
sha256sums=('SKIP')

package() {
  cd "$srcdir/$pkgname"
  cp -a etc "$pkgdir/"
}
